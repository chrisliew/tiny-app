<!DOCTYPE html>
<html lang="en">
<!-- Used to display URL and all it's shortened forms -->

<header>
  <% include ./partials/_header %>
</header>

<head>
  <title>TinyApp</title>
</head>

<body>
  <p>Shortened and Full Length URL Table</p>

<!--   This table is fed data from the render urls_index.  It loops through the URLDatabase and returns a table.
  The first column of the table returns the shortened URL (Key of the object)
  The second column of the table returns the full URL. Third column is edit link and fourth is delete.-->
  <table>
      <% for(var keys in urls) { %>
        <tr>
          <td><a href="Placeholder">Placeholder link for shortened URL</a><td>
          <td><%= cookies %></td>
          <td><%= keys %></td>
          <td><%= urls[keys] %></td>
          <td><a href="/urls/<%=keys%>">Edit</a></td>
          <td><form method="POST" action="/urls/<%=keys%>/delete">
              <button type="submit">Delete</button></form></td>
        </tr>
      <% } %>
    </table>
</body>
</html>




